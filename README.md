## Problems

1. The biggest complaint I get about docker is that it's hard to run commands. This problem becomes even more extreme with agents.

## Dockerfile

/code - source of the package
/monorepo - the full monorepo

Build from the root of the monorepo


## Install

```
brew install direnv
```